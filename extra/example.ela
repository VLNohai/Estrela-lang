logic append(ls1, ls2, rezme)
    ({}, ls2, ls2) end
    ({h|t}, ls2, rezme)
        append(t, ls2, rez_rec);
        rezme = {h|rez_rec};
    end
end

logic test(list, rez)
    (list, rez) 
        append({1, 2, 3}, list, rez_rec);
        rez = rez_rec;
    end
    (list, rez) 
        append({2}, list, rez_rec);
        rez = rez_rec
    end
    (list, rez) 
        append({3}, list, rez_rec);
        rez = rez_rec
    end
end